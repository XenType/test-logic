export const testFunctionAsync = async (a: number, b: string): Promise<any> => {
    return;
};
export const testFunction = (a: number, b: string): any => {
    return;
};
export const calledFunction = (a: number, b: string, c: boolean): any => {
    return c;
};
